@using Microsoft.AspNet.Identity
@if (User.IsInRole("expert"))
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { @class = "navbar-left" }))
    {
        @Html.AntiForgeryToken()

    <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown ">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                ДТТ
            </a>
            <div class="dropdown-menu">
                @Html.ActionLink("Проверка работ", "ExpertWorkView", "CheckStudentExam", null, new { @class = "dropdown-item" })
                <div class="dropdown-divider"></div>
                @Html.ActionLink("Проверенные работы", "CheckedStudentExams", "CheckStudentExam", null, new { @class = "dropdown-item" })
                @Html.ActionLink("Статистика эксперта", "Stats", "CheckStudentExam", null, new { @class = "dropdown-item" })
                @Html.ActionLink("Статистика по баллам", "StatByQuestion", "CheckStudentExam", null, new { @class = "dropdown-item" })
            </div>
        </li>
    </ul>
    }
}
else
{

}

